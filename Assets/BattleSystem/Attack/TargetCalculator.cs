using System.Collections.Generic;
using FormationSystem;
using UnitSystem;

namespace BattleSystem.Attack
{
    public static class TargetCalculator
    {
        public static List<FormationPosition> GetValidTargets(Unit unit, FormationManager otherFormation)
        {
            var targets = new List<FormationPosition>();
            
            // var position = FormationPosition.BackLeft.GetN
            return targets;
        }
    }
}